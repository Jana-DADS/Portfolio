# Session Logs

Projects keep session logs in `./session_logs/` at the project root.

- **At session start**: check the log directory and read the most recent
  log(s) relevant to what we're working on — that's how you recover context
  from previous sessions.
- **Writing**: only when the user explicitly asks ("write a session log").
  Use the `session-log` skill.
